apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: bm-kubeletconfig
  namespace: openshift-gitops
  labels:
    type: bootstrap
spec:
  destination:
    name: bm
  project: bm
  source:
    directory:
      jsonnet: {}
      recurse: true
    path: cluster-go/bootstrap/KubeletConfig
    repoURL: https://github.com/joelapatatechaude/hub
  syncPolicy:
    automated: {}
    syncOptions:
    - Prune=true
