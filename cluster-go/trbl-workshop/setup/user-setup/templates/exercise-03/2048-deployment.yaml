{{- if .Values.userCount | int }}
{{- $userCount := .Values.userCount | int }}
{{- range $index := until $userCount }}
---
kind: Deployment
apiVersion: apps/v1
metadata:
  annotations:
    alpha.image.policy.openshift.io/resolve-names: '*'
    app.openshift.io/route-disabled: 'false'
  name: puzzle2051
  namespace: exercise03-user{{ printf "%02d" (add $index 1) }}
  labels:
    app: puzzle2051
    app.kubernetes.io/component: puzzle2051
    app.kubernetes.io/instance: puzzle2051
    app.kubernetes.io/name: puzzle2051
    app.openshift.io/runtime: nginx
    app.openshift.io/runtime-namespace: exercise03-user{{ printf "%02d" (add $index 1) }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: puzzle2051
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: puzzle2051
        deployment: puzzle2051
      annotations:
        openshift.io/generated-by: OpenShiftWebConsole
    spec:
      containers:
        - name: puzzle2051
          image: 'quay.io/rh_ee_cschmitz/puzzle2048:latest'
          resources:
            limits:
              cpu: '1'
              memory: 300Mi
            requests:
              cpu: '1'
              memory: 300Mi
          ports:
            - containerPort: 8080
              protocol: TCP
          resources: {}
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
          imagePullPolicy: Always
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst
      securityContext: {}
      schedulerName: default-scheduler
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 25%
      maxSurge: 25%
  revisionHistoryLimit: 10
  progressDeadlineSeconds: 600
{{- end }}
{{- end }}
